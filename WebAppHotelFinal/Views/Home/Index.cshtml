@{
    ViewData["Title"] = "Home Page";
}

<div class="container mt-5">
    <!-- Hero Section -->
    <div class="jumbotron p-5 rounded shadow-sm bg-light text-center">
        <h1 class="display-4 mb-3">Добре дошли в нашия хотел!</h1>
        <p class="lead mb-4">
            Резервирайте стая лесно и бързо, следете вашите резервации и управлявайте хотелските ресурси.
        </p>

        <div class="d-flex justify-content-center gap-3 flex-wrap">
            <a asp-controller="Rooms" asp-action="Index" class="btn btn-primary btn-lg">
                Виж стаите
            </a>

            @if (User.Identity.IsAuthenticated)
            {
                <a asp-controller="Reservations" asp-action="Create" class="btn btn-success btn-lg">
                    Направи резервация
                </a>
            }

            @if (User.IsInRole("Admin"))
            {
                <a asp-controller="Rooms" asp-action="Create" class="btn btn-warning btn-lg">
                    Добави нова стая
                </a>
            }
        </div>
    </div>

    <!-- Info Section -->
    <div class="row mt-5">
        <div class="col-md-4 text-center mb-4">
            <i class="bi bi-house-fill display-4 mb-2 text-primary"></i>
            <h4>Комфортни стаи</h4>
            <p>Всички стаи са модерно обзаведени и оборудвани за вашия комфорт.</p>
        </div>

        <div class="col-md-4 text-center mb-4">
            <i class="bi bi-calendar-check-fill display-4 mb-2 text-success"></i>
            <h4>Лесна резервация</h4>
            <p>Резервирайте стая само с няколко клика, вижте наличните дати и цени.</p>
        </div>

        <div class="col-md-4 text-center mb-4">
            <i class="bi bi-people-fill display-4 mb-2 text-warning"></i>
            <h4>Управление на хотел</h4>
            <p>Админ панел за управление на стаи, резервации и клиенти.</p>
        </div>
    </div>
</div>
